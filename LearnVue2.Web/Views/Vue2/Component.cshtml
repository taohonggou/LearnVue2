
@{
    ViewBag.Title = "Component";
}

<div id="app">
    <h2>Component</h2>
    <input type="text" placeholder="" />

    <custom-input placeholder="输入款"
                  v-bind:type="inputType"
                  v-model="inputValue"></custom-input>

</div>


@section scripts{
    <script type="text/javascript">
        /**
         * 要实现的功能就是
         * 1.实现长度的控制
         * 2.实现规则的校验
         */
        Vue.component('custom-input', {
            template: '\
              <input\
                ref="input"\
                v-bind:value="value"\
                v-on:input="updateValue($event.target.value)"\
                v-bind:max-length="maxLength"\
              >\
          ',
            prop: {
                maxLength: {
                    type: Number,
                    required: false,
                }
            },
            method: {
                updateValue: function (value) {

                }
            }

        });

        var vm = new Vue({
            el: '#app',
            data: {
                inputType: 'text',
                inputValue: 'abc',
            }
        });
    </script>


}
